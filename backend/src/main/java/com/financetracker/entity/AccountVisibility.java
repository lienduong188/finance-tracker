package com.financetracker.entity;

public enum AccountVisibility {
    PRIVATE,           // Only owner can see
    FAMILY_VISIBLE,    // All family members can see
    SPECIFIC_MEMBERS   // Only specified members can see
}
