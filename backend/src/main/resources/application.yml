spring:
  application:
    name: finance-tracker-api

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:finance_tracker}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: ${SERVER_PORT:8080}

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:your-256-bit-secret-key-here-make-it-long-enough-for-hs256}
  expiration: ${JWT_EXPIRATION:86400000}  # 24 hours in milliseconds
  refresh-expiration: ${JWT_REFRESH_EXPIRATION:604800000}  # 7 days

# OpenAPI/Swagger
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

# Logging
logging:
  level:
    com.financetracker: DEBUG
    org.springframework.security: DEBUG

# CORS
cors:
  allowed-origins: http://localhost:3000,http://localhost:5173

# Groq AI Configuration
groq:
  api-key: ${GROQ_API_KEY:}
  api-url: https://api.groq.com/openai/v1/chat/completions
  model: llama-3.3-70b-versatile

# AI Limits (global) - Based on Groq Free Tier
ai:
  limits:
    # Groq Free Tier: 30 RPM, 14,400 RPD
    requests-per-day: ${AI_REQUESTS_PER_DAY:14400}
    requests-per-minute: ${AI_REQUESTS_PER_MINUTE:30}

# Exchange Rate Configuration
exchange-rate:
  api-url: https://api.frankfurter.app
  update-cron: "0 0 * * * *"  # Every hour
  cache-duration-minutes: 60
  fallback-enabled: true

# Email Configuration (optional - app will run without SMTP)
spring.mail:
  host: ${SMTP_HOST:}
  port: ${SMTP_PORT:587}
  username: ${SMTP_USER:}
  password: ${SMTP_PASS:}

# App Configuration
app:
  mail:
    from: ${MAIL_FROM:noreply@resend.dev}
    resend-api-key: ${RESEND_API_KEY:}
    verification-url: ${APP_URL:http://localhost:5173}/verify-email
    reset-password-url: ${APP_URL:http://localhost:5173}/reset-password
    verification-token-expiry-hours: 24
